{% set name = "mt-metadata" %}
{% set version = "0.2.3" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/mt_metadata-{{ version }}.tar.gz
  sha256: be99f5ac79cf639f705b8da45ac54984481b3b6f9bbc46308c243bc66a46073b

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - pytest-runner
    - python >=3.5
  run:
    - concurrent-log-handler
    - matplotlib-base
    - numpy <=1.21
    - obspy
    - pandas
    - python >=3.5
    - pyyaml
    - xarray

test:
  imports:
    - mt_metadata
    - mt_metadata.base
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/kujaku11/mt_metadata
  summary: Metadata for magnetotelluric data
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - kujaku11
    - kkappler
    - lheagy
